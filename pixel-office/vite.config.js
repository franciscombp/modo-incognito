import { defineConfig } from "vite";

// Served from https://<user>.github.io/modo-incognito/ by GitHub Pages,
// so all asset URLs need the repo name as a base path.
export default defineConfig({
  base: "/modo-incognito/",
});
